export type TrainComponentRequest = {
    number: string,
    description: string
};

export type TrainSetRequest = {
    number: string,
    year: number
};

export type TrainArticleRequest = {
    component: string
};
